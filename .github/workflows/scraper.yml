name: Wallapop Scraper Automation

on:
  schedule:
    - cron: '0 4 * * *'  # 6:00 AM España
  
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Modo de prueba (solo GESTICAR G.)'
        required: false
        default: false
        type: boolean
      test_type:
        description: 'Tipo de vendedor para prueba'
        required: false
        default: 'gesticar'
        type: choice
        options:
          - gesticar
          - dursan
          - multiple

jobs:
  # JOB 1: Todo lo que SI completo en 6h (5,163 vehiculos, ~5h 45m)
  scrape-completed-vendors:
    runs-on: ubuntu-latest
    timeout-minutes: 350  # 5h 50m (margen de seguridad)
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Install Chrome and ChromeDriver
      run: |
        sudo apt-get update
        wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
        sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable xvfb
        
    - name: Setup Virtual Display
      run: |
        Xvfb :99 -screen 0 1920x1080x24 &
        echo "DISPLAY=:99" >> $GITHUB_ENV
        
    - name: Run Scraper (Vendors that completed)
      env:
        GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
        GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        HEADLESS_MODE: true
        TEST_MODE: ${{ inputs.test_mode || 'false' }}
        TEST_TYPE: ${{ inputs.test_type || 'gesticar' }}
        VENDOR_GROUP: 'job1'
      run: |
        cd src
        python COCHES_SCR.py
        
    - name: Upload Artifacts (Completed Vendors)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: scraper-completed-${{ github.run_number }}
        path: |
          src/resultados/
          src/logs/
        retention-days: 30

  # JOB 2: Solo Flexicar + INTEGRAL MOTION (3,455 vehiculos, ~5h 30m)
  scrape-remaining-vendors:
    runs-on: ubuntu-latest
    timeout-minutes: 350  # 5h 50m (margen de seguridad)
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Install Chrome and ChromeDriver
      run: |
        sudo apt-get update
        wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
        sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable xvfb
        
    - name: Setup Virtual Display
      run: |
        Xvfb :99 -screen 0 1920x1080x24 &
        echo "DISPLAY=:99" >> $GITHUB_ENV
        
    - name: Run Scraper (Remaining Vendors)
      env:
        GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
        GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        HEADLESS_MODE: true
        TEST_MODE: ${{ inputs.test_mode || 'false' }}
        TEST_TYPE: ${{ inputs.test_type || 'gesticar' }}
        VENDOR_GROUP: 'job2'
      run: |
        cd src
        python COCHES_SCR.py
        
    - name: Upload Artifacts (Remaining Vendors)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: scraper-remaining-${{ github.run_number }}
        path: |
          src/resultados/
          src/logs/
        retention-days: 30

  # JOB 3: ANALISIS HISTORICO (se ejecuta despues de ambos scraper jobs)
  analyze-historical-data:
    runs-on: ubuntu-latest
    needs: [scrape-completed-vendors, scrape-remaining-vendors]  # Espera a que terminen ambos jobs
    timeout-minutes: 30  # 30 minutos deberia ser suficiente para el analisis
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run Historical Analysis
      env:
        GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
        GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        TEST_MODE: ${{ inputs.test_mode || 'false' }}
      run: |
        cd src
        python analisis_coches.py
        
    - name: Upload Analysis Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: analysis-results-${{ github.run_number }}
        path: |
          src/logs/
        retention-days: 30
        
    - name: Notify Final Completion
      if: always()
      run: |
        echo "SCRAPER + ANALISIS: Proceso completo finalizado"
        echo "DATOS: Revisar Google Sheets para datos completos"
        echo "JOB 1: ~5,163 vehiculos procesados"
        echo "JOB 2: ~3,455 vehiculos procesados"
        echo "ANALISIS: Historico de precios actualizado"
        echo "HOJA: Data_Historico contiene el historico evolutivo completo"
